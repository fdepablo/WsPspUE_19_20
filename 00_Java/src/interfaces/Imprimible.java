package interfaces;

public interface Imprimible {
	/**
	 * Manda la cadena entrante al dispositivo para 
	 * mostrarla
	 * @param cadena, el string a imprimir
	 */
	public void imprimir(String cadena);
}
